debug: true
projectName: pgs
build:
  images:
    - name: shell-nsi
deploy:
  apps:
    - name: shell-nsi
    - name: shell-nsi-solar
      image: shell-nsi
      appDeltaConfigName: config_delta_solar.yaml